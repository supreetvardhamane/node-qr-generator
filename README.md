# 🔳 node-qr-generator

A simple Node.js CLI app that converts a user-provided URL into a QR code image and saves the URL locally in a text file. Built using npm packages for interactive input and QR generation.


## 🚀 Setup & Run

1. Make sure you have **Node.js (v18+)** installed.
2. Clone the repository:

```bash
git clone https://github.com/supreetvardhamane/node-qr-generator.git
cd node-qr-generator
```

3. Install dependencies:

```bash
npm install inquirer qr-image
```

4. Run the app:

```bash
node index.js
```

Enter a URL when prompted.


## 📁 Output

- `qr_image.png` → Generated QR code  
- `url.txt` → Saved URL  
